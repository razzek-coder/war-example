pipeline {
  agent any

  tools {
    maven 'maven 3.9.9'
  }

  stages {
    stage('Build') {
      steps {
        // war-example-0.0.1.war
        bat 'mvn -B -q clean install'
      }
    }
  }
}
